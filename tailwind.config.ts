/**
 * Tailwind CSS v4 Configuration
 *
 * Tailwind v4 uses CSS-first configuration via @layer theme in your CSS files.
 * This TypeScript config file is kept for:
 * - TypeScript type checking
 * - Any JavaScript-specific configuration if needed
 *
 * Most theme customization should be done in app/globals.css using @layer theme
 *
 * @see https://tailwindcss.com/blog/tailwindcss-v4
 */
const config = {
  // Content detection is automatic in v4
  // Tailwind automatically discovers template files based on heuristics
  // You can use @source directive in CSS if needed: @source "../custom-path"
};

export default config;
